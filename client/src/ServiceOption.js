import React from 'react';

function ServiceOption(price, number_of_sessions, frequency, addition) {

    return (
        <div className="service-option">
            <p>Price: ${}</p>
        </div>
    )
}

export default ServiceOption;